# Deploy Final MORBIT

Use the full project ZIP and preserve your real:
- knowledge/NFA/
- knowledge/PFSA/

```bash
git add .
git commit -m "Finalize MORBIT right rail and death scene vision priority"
git pull --rebase origin main
git push origin main
```

Streamlit Community Cloud:
1. Confirm branch `main`.
2. Confirm main file `app.py`.
3. Reboot once.
4. Open a fresh browser session and start a new case.
